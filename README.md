# House-prices-prediction
This project predicts house prices using machine learning. It cleans real-world data by removing missing values, duplicates, and outliers, then trains a Linear Regression model on features such as age, rooms, size, location, and income. Users can enter details to instantly get a price estimate.
